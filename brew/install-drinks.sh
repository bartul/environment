brew install tree # Displays directory structures as a tree
brew install watch # Runs a command repeatedly and shows updated output
brew install trash # Moves files to the macOS Trash from the command line
brew install openssl # Provides TLS and cryptography libraries and tools
brew install telnet # Provides the Telnet client for testing raw TCP connections

brew install vim # Installs the Vim terminal text editor
brew install curl # Transfers data with URLs from the command line
brew install wget # Downloads files from HTTP, HTTPS, and FTP sources
brew install git # Provides distributed version control
brew install diff-so-fancy # Improves Git diff readability in the terminal
brew install fish # Installs the Fish interactive shell
brew install fisher # Installs a plugin manager for the Fish shell
brew install jq # Parses and transforms JSON from the command line

brew install starship # Provides a fast, customizable cross-shell prompt
brew install font-caskaydia-mono-nerd-font # Installs Caskaydia Mono Nerd Font with developer icons
brew install font-jetbrains-mono-nerd-font # Installs JetBrains Mono Nerd Font with developer icons
brew install font-fira-code-nerd-font # Installs Fira Code Nerd Font with developer icons

brew install elm # Installs the Elm language compiler and tools
brew install node # Installs the Node.js JavaScript runtime and npm
brew install wrk # Installs an HTTP benchmarking tool

brew install nyancat # Displays the animated Nyan Cat in the terminal

brew install mas # Installs a command-line interface for the Mac App Store

brew install dotnet-sdk # Installs the .NET SDK for building and running .NET apps
brew install visual-studio-code # Installs Microsoft's Visual Studio Code editor

brew install azure-cli # Installs Microsoft's Azure command-line interface

brew install kubernetes-cli # Installs kubectl for managing Kubernetes clusters
brew install kubectx # Switches quickly between Kubernetes contexts and namespaces
brew install kubelogin # Provides authentication helpers for Kubernetes clusters
brew install kind # Runs local Kubernetes clusters in Docker containers
brew install helm # Installs the Kubernetes package manager
brew install helmfile # Manages declarative Helm chart releases
brew install k9s # Provides a terminal UI for managing Kubernetes clusters

brew install gh # Installs the GitHub command-line interface

brew install chromium # Installs the Chromium web browser
brew install transmission # Installs the Transmission BitTorrent client
brew install rectangle # Adds keyboard-driven window management for macOS

brew install iterm2 # Installs the iTerm2 terminal emulator

brew tap microsoft/git # Adds Microsoft's Homebrew tap for Git tooling
brew install git-credential-manager-core # Installs Microsoft's Git Credential Manager

brew install font-fira-code # Installs the Fira Code programming font
brew install font-jetbrains-mono # Installs the JetBrains Mono programming font
brew install font-cascadia-code # Installs the Cascadia Code programming font
brew install font-cascadia-code-pl # Installs Cascadia Code PL with Powerline glyphs
